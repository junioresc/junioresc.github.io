import React from 'react';
import Nav from '../Nav';

function Header() {
    
    return (
        <header>
            <h1>
                <a href="/">Junior Escobar</a>
            </h1>
        </header>
    );
}

export default Header;